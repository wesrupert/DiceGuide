<Page
    x:Class="DiceGuide.CharacterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DiceGuide"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Pivot >
            <PivotItem Header="General">
                <StackPanel>
                    <TextBlock Text="{x:Bind Path=Character.Name, Mode=OneWay}" />
                    <TextBlock>
                        Level <Run Text="{x:Bind Character.Level}" /> <Run Text="{x:Bind Character.Race.Name}" />
                    </TextBlock>
                    <TextBlock Text="{x:Bind Path=Character.Text, Mode=OneWay}" />
                </StackPanel>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
